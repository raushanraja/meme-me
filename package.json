{
  "name": "meme-me",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "npm run lint && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "prettier": "3.1.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "face-api.js": "^0.22.2"
  }
}
